# compare_test_010 验收记录

任务名称：商品对比页面测试用例与验收验证
当前步骤：5 - 编写重复添加与数量上限测试
任务ID：compare_test_010

## 本次交付范围
- 补充商品对比页面“重复添加”测试
- 补充商品对比页面“超过数量上限”测试
- 区分两类上限处理路径：
  - 前端在到达上限时直接拦截
  - 前端允许触发添加，但以后端上限响应完成拦截

## 执行方式
在现有 Vitest + Testing Library 测试体系内执行页面级测试，结合接口 mock 验证：
- 前端提示
- 列表去重
- 接口调用次数与参数
- 超上限时已有数据稳定性
- 按钮禁用或继续可交互状态

## 验收覆盖点
1. 重复添加同一商品时，列表不出现重复项
2. 重复添加场景下，前端提示和接口处理结果符合现有实现
3. 超过数量上限时，限制提示、按钮状态和现有数据保持正确
4. 若上限规则未明确，测试结果中清楚标注待确认项

## 待确认项
- 对比数量上限值：若代码中未以常量、配置或文案明确暴露，测试应读取现有实现约束；若仍无法确认，则保持为待确认项，不在用例中硬编码业务值
- 超上限的最终提示文案：应以现有实现的 aria/text/testid 为准，必要时在测试中使用宽松匹配避免误绑文案细节

## 建议执行命令
- `pnpm vitest run compare`
- 或按仓库现有脚本执行商品对比页测试子集
